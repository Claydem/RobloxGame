---
name: test-runner-and-fixer
description: >-
  Autonomously runs the project's test suite, analyzes failures, pinpoints root causes, applies minimal bug fixes,
  and loops until all tests pass with zero regressions.
  Use when tests are failing, before submitting changes, or when validating refactoring.
---

# Autonomous Test Runner and Fixer for Roblox
Runs test suites (`selene`, `testez`, `rokit`), diagnoses errors, and fixes code until 100% passing.
